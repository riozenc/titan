server:
 port: 9922
spring:
 application:
  name: gateway
 cloud:
  gateway:
   discovery:
    locator:
     enabled: true
eureka:
  instance: 
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://enn:df6100@127.0.0.1:7777/eureka/
logging:
  level:
    org.gateway: info
    org.springframework.cloud.gateway: info
  path: log