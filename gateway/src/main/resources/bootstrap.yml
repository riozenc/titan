server:
 port: 9911
spring:
 application:
  name: gateway
 cloud:
  gateway:
   discovery:
    locator:
     enabled: true
eureka:
  instance: 
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://titan:df6100@127.0.0.1:7771/eureka/
logging:
  level:
    org.gateway: debug
    org.springframework.cloud.gateway: debug
  path: log