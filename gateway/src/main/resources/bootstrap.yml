spring:
 application:
  name: gateway
 cloud:
  config:
   profile: dev
   label: master
   discovery:
    enabled: true
    service-id: config-center
eureka:
  instance: 
    appGroupName: GATEWAY
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://titan:df6100@172.21.29.55:7777/eureka/