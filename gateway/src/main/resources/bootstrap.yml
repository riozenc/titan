server:
 port: 9911
spring:
 application:
  name: gateway-service
 cloud:
  gateway:
   discovery:
    locator:
     enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7777/eureka/
logging:
  level:
    org.springframework.cloud.gateway: debug